---
import { cn } from "@/utils/commonFunctions"
const { addtionalClasses } = Astro.props;
const baseClasses = "container mx-auto p-4 sm:p-6 max-w-3xl";

---
<div class={cn(baseClasses, addtionalClasses)}>
  <slot />
</div>