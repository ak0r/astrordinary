---
import BaseLayout from "@/layouts/BaseLayout.astro"
import Container from "@/components/Container.astro";
import { pageAbout, SiteMeta } from "@/configs/consts";

const pageTitle = {
	title: `${pageAbout.pageTitle}` || "Title" ,
  subTitle: `${pageAbout.pageSubtitle}` || "Sub-Title",
};

---

<BaseLayout pageTitle=`${pageTitle.title}` pageDesc=`${pageTitle.subTitle}`>
	<Container className="flex my-4">

	</Container>
</BaseLayout>